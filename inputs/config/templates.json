{
     // comments are allowed

    "logging": {
        "loglevel" : "trace",
        "logfile": "logfile.txt"
    },
    "paths": {
        "outputDirectory": "outputs/templates"
    },
    "templates" : {
        "engine": "./build-Desktop-Debug/libEngine-Gen1.so"
    }
}
