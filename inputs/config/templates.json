{
     // comments are allowed

    "logging": {
        "loglevel" : 0,
        "logfile": "logfile.txt"
    },
    "templates" : {
        "engine": "./build-Desktop-Debug/libEngine-Gen1.so",
        "outputDirectory": "outputs"
    }
}
