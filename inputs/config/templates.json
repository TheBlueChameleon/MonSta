{
     // comments are allowed

    "logging": {
        "loglevel" : "trace",
        "logfile": "logfile.txt"
    },
    "paths": {
        "outputDirectory": "outputs/templates",
        "inputDirectory" : "inputs",
        "engine": "engines/libEngine-Gen1.so"
    },
    "templates" : {
        //"engine": "foo"
    }
}
